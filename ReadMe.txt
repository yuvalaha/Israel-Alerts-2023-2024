# Alerts Application

The `alerts.json` file is updated as of **October 11, 2024**.

## How to Run the Program

Follow these steps to get started:

1. **Install Dependencies**  
   Run the following command to install the required packages:
   ```bash
   npm install
   ```

2. **Navigate to the Application Directory**  
   Change to the application directory:
   ```bash
   cd ./notes-app/
   ```

3. **Run the Application**  
   Execute the application using the following command, replacing `"נתניה - מזרח"` with the desired city:
   ```bash
   node app.js read --city="נתניה - מזרח"
   ```
